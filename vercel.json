{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "docs/.vitepress/dist",
        "buildCommand": "pnpm docs:build"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/$1" }]
}
